# renewalops

Staff signs in
→ Adds a client
→ Adds the client’s contact
→ Creates a maintenance contract
→ Adds a renewal date
→ Selects reminder dates
→ Sends a test email
→ Views the email status
→ Marks the contract as renewed
